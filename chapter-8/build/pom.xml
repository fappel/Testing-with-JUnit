<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>book.twju</groupId>
  <artifactId>build</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>build</name>
  <url>http://maven.apache.org</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <ui.test.vmargs></ui.test.vmargs>
    <revision>0.0.1-SNAPSHOT</revision>
    <junit-version>4.12</junit-version>
    <assertj-version>3.0.0</assertj-version>
    <mockito-version>1.10.19</mockito-version>
    <burst-version>1.1.0</burst-version>
    <junitparams-version>1.0.4</junitparams-version>
    <cp-suite-version>1.2.7</cp-suite-version>
  </properties>

  <modules>
    <module>../timeline</module>
    <module>../timeline.swing</module>
    <module>../timeline.swing.application</module>
    <module>../timeline.swt</module>
    <module>../timeline.swt.application</module>
    <module>../timeline.swt.test.util</module>
    <module>../timeline.tabris</module>
    <module>../timeline.provider</module>
    <module>../timeline.test.util</module>
    <module>../timeline.util</module>
    <module>../timeline.util.test.util</module>
  </modules>

  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit-version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>${assertj-version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-all</artifactId>
      <version>${mockito-version}</version>
      <scope>test</scope>
    </dependency>
		<dependency>
		  <groupId>com.squareup.burst</groupId>
		  <artifactId>burst-junit4</artifactId>
		  <version>${burst-version}</version>
		  <scope>test</scope>
		</dependency>
		<dependency>
      <groupId>pl.pragmatists</groupId>
      <artifactId>JUnitParams</artifactId>
      <version>${junitparams-version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.takari.junit</groupId>
      <artifactId>takari-cpsuite</artifactId>
      <version>${cp-suite-version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.3</version>
        <configuration>
          <source>1.8</source>
          <target>1.8</target>
        </configuration>
      </plugin>
      
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>2.12.4</version>
          <configuration>
            <!-- This is required for code coverage to work. -->
            <argLine>${surefireArgLine} ${ui.test.vmargs}</argLine>      
          </configuration>
      </plugin>
      
      <plugin>
        <groupId>org.eluder.coveralls</groupId>
        <artifactId>coveralls-maven-plugin</artifactId>
        <version>3.1.0</version>
        <configuration>
          <repoToken>${env.repoToken}</repoToken>
        </configuration>
      </plugin>
      
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.7.4.201502262128</version>
        <configuration>
          <excludes>
            <!-- Exclude measurement of platform specific timeline wirings and test util classes -->
            <exclude>**/Application.class</exclude>
            <exclude>**/Configuration.class</exclude>
            <exclude>**/TimelineEntryPoint.class</exclude>
            <exclude>book/twju/timeline/test/util/*</exclude>
          </excludes>
        </configuration>
        <executions>
		       <execution>
		         <id>pre-unit-test</id>
		         <goals>
		            <goal>prepare-agent</goal>
		         </goals>
		         <configuration>
		            <destFile>${project.build.directory}/coverage-reports/jacoco.exec</destFile>
		            <propertyName>surefireArgLine</propertyName>
		         </configuration>
		      </execution>
		      <execution>
		         <id>post-unit-test</id>
		         <phase>test</phase>
		         <goals>
		            <goal>report</goal>
		         </goals>
		         <configuration>
		            <dataFile>${project.build.directory}/coverage-reports/jacoco.exec</dataFile>
		            <outputDirectory>${project.reporting.outputDirectory}/jacoco</outputDirectory>
		         </configuration>
		      </execution>
		      <execution>
		         <id>default-report</id>
		         <phase>prepare-package</phase>
		         <goals>
		            <goal>report</goal>
		         </goals>
		      </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>